{
    "master": {
        "prefix": {
            "description": "The custom prefix for Skyra.",
            "type": "String",
            "default": "s!"
        },
        "language": {
            "description": "The language in which Skyra will display the messages.",
            "type": "Language",
            "default": "en-US"
        },
        "mode": {
            "description": "The mode Skyra should operate on.\n0 means Free, which allows the use of any command.\n1 means Lite, which disables a bunch of commands.\n2 means Strict, which limits all the commands for the server to the moderation module and system ones.\nPlease, use this instead of making Skyra ignore a long list of commands.",
            "type": "Integer",
            "default": 0,
            "max": 3,
            "min": 0
        },
        "ignoreChannels": {
            "description": "The channels you want me to ignore when giving points (Social Module).",
            "type": "Channel",
            "default": [],
            "array": true
        }
    },
    "disable": {
        "cmdChannels": {
            "description": "The channels you want me to ignore when executing commands (with exception of the moderation commands), use this instead of removing the SEND MESSAGES permissions in the channel, unless you want to waste more time going to another channel. Skyra ignores all the channels she can send messages on.",
            "type": "Channel",
            "default": [],
            "array": true
        },
        "commands": {
            "description": "The commands you want me to disable.",
            "type": "Command",
            "default": [],
            "array": true
        }
    },
    "roles": {
        "admin": {
            "description": "The Administrator Role, defaults to whoever has the ADMINISTRATOR permission, they get granted a Permission Level 3 and a wide range of commands.",
            "type": "Role"
        },
        "moderator": {
            "description": "The Moderator Role, defaults to whoever has the BAN MEMBERS permission. They get granted a Permission Level 2 and a selection of moderation commands.",
            "type": "Role"
        },
        "staff": {
            "description": "The Staff Role. They get granted a Permission Level 1 and some harmless commands that help moderating.",
            "type": "Role"
        },
        "muted": {
            "description": "The Muted Role. They get all their roles temporary removed even if the user rejoins.",
            "type": "Role"
        },
        "initial": {
            "description": "The Initial Role. Every new member gets granted this role after joining the server.",
            "type": "Role"
        },
        "subscriber": {
            "description": "A Subscriber role, you can use this role to do announcements (throughtout the announcement command), Skyra will keep the role unmentionable, and toggle it quickly before sending the announcement.",
            "type": "Role"
        },
        "public": {
            "description": "The public roles, every role here will be claimable throughout Skyra, make sure she has permissions to give them.",
            "type": "Role",
            "default": [],
            "array": true
        }
    },
    "events": {
        "banAdd": {
            "description": "Posts a modlog when a user gets banned from the server.",
            "type":"Boolean",
            "default": false
        },
        "banRemove": {
            "description": "Posts a modlog when a user gets unbanned from the server.",
            "type":"Boolean",
            "default": false
        },
        "memberAdd": {
            "description": "Posts a log when a user joins the server.",
            "type":"Boolean",
            "default": false
        },
        "memberRemove": {
            "description": "Posts a log when a user leaves the server.",
            "type":"Boolean",
            "default": false
        },
        "memberNicknameChange": {
            "description": "Posts a log when a member gets the nickname changed.",
            "type":"Boolean",
            "default": false
        },
        "memberRolesChange": {
            "description": "Posts a log when a member gets their roles changed.",
            "type":"Boolean",
            "default": false
        },
        "messageDelete": {
            "description": "Posts a log when a message gets deleted.",
            "type":"Boolean",
            "default": false
        },
        "messagePrune": {
            "description": "Posts a log when a prune is executed.",
            "type":"Boolean",
            "default": false
        },
        "messageEdit": {
            "description": "Posts a log when a message gets edited.",
            "type":"Boolean",
            "default": false
        },
        "commands": {
            "description": "Post a log when a command gets executed. This event tells who executed it, in which channel, and when.",
            "type":"Boolean",
            "default": false
        }
    },
    "channels": {
        "announcement": {
            "description": "The announcement channel Skyra should use to post the messages from 'Skyra, announce'.\nPD: Announcements are made by server administrators, and mentions the subscriber role if configured.",
            "type": "TextChannel"
        },
        "default": {
            "description": "The channel Skyra will use to send greetings and farewells.",
            "type": "TextChannel"
        },
        "log": {
            "description": "The channel in which normal logs are sent.",
            "type": "TextChannel"
        },
        "modlog": {
            "description": "The modlog channel. When a moderator or administrator use a moderation command (ban, unban, softban, kick...), a special kind of log will be posted there. Modlogs contain five fields: 'Moderator', 'Type', 'User', 'Reason' and 'Case', with exception of the anti-invite logs.",
            "type": "TextChannel"
        },
        "messagelogs": {
            "description": "The message logs channel. This is the channel where Skyra will post the logs related to messages (message delete or edit).",
            "type": "TextChannel"
        },
        "spam": {
            "description": "The spam channel, if set, a set of selected commands (the most spammy/funny ones) will only work there.",
            "type": "TextChannel"
        },
        "starboard": {
            "description": "The starboard channel!",
            "type": "TextChannel"
        }
    },
    "messages": {
        "greeting": {
            "description": "A custom message for greetings. If not set, Skyra won't send messages to the users who join the server. Templates are:\n• %MEMBER% : Mentions the user.\n• %MEMBERNAME% : Places the user's username.\n• %GUILD% : Places the server's name.",
            "type": "string"
        },
        "farewell": {
            "description": "A custom message for farewells. If not set, Skyra won't send messages to the users who leave the server. Templates are:\n• %MEMBER% : Mentions the user.\n• %MEMBERNAME% : Places the user's username.\n• %GUILD% : Places the server's name.",
            "type": "string"
        },
        "warnings": {
            "description": "Whether Skyra should or not send a message after sending a warning.",
            "type": "Boolean",
            "default": false
        }
    },
    "twitch": {
        "channel": {
            "description": "The channel to post when a user starts/stops streaming.",
            "type": "TextChannel"
        },
        "messagestart": {
            "description": "A custom message for Twitch messages when the user starts streaming. Requires channels::stream enabled.",
            "type": "string"
        },
        "messagestop": {
            "description": "A custom message for Twitch messages when the user stops streaming. Requires channels::stream enabled.",
            "type": "string"
        },
        "list": {
            "description": "The whitelist or blacklist containing the twitch profiles to post the information from.",
            "type": "string",
            "default": [],
            "array": true
        },
        "mode": {
            "description": "The mode the twitch module should operate at.\n0 - To disable the twitch module.\n1 - To enable all links,\n2 - To enable a blacklist which will post any link but the ones in the list.\n3 - To enable a whitelist which will post only the links that are in the list.",
            "type": "Integer",
            "default": 0,
            "min": 0,
            "max": 3
        }
    },
    "selfmod": {
        "invitelinks": {
            "description": "Whether Skyra should or not remove messages which contain an invite link.",
            "type": "Boolean",
            "default": false
        },
        "nomentionspam": {
            "description": "Whether the NoMentionSpam3 system should be enabled or not. This system adds temporal points to users and ban them in case they reach the threshold.",
            "type": "Boolean",
            "default": false
        },
        "nmsthreshold": {
            "description": "This key requires the 'nomentionspam' key enabled. The amount of points required to ban a user.\nNote: An everyone/here tag counts as 4 points, role mentions as 2, and each member mentioned, 1.",
            "type": "Integer",
            "default": 20,
            "min": 10
        },
        "raid": {
            "description": "Whether the Anti-RAID protection should be enabled or not. This system takes action when a server gets too many new users in a frame of 20 seconds.",
            "type": "Boolean",
            "default": false
        },
        "raidthreshold": {
            "description": "This key requires the 'raid' key enabled. This key specifies the amount of new users in a frame of 20 seconds needed to trigger the Anti-RAID protection.",
            "type": "Integer",
            "default": 10,
            "max": 50,
            "min": 5
        }
    },
    "filter": {
        "level": {
            "description": "",
            "type": "Integer",
            "default": 0,
            "min": 0,
            "max": 3
        },
        "raw": {
            "description": "",
            "type": "String",
            "default": [],
            "array": true
        }
    },
    "social": {
        "achieve": {
            "description": "Whether Skyra should notify levelups or not.",
            "type": "Boolean",
            "default": false
        },
        "achieveMessage": {
            "description": "The achievement message. This key requires social::achieve and is defaulted with a multilanguage message.",
            "type": "String",
            "min": 0,
            "max": 500
        }
    }
}
